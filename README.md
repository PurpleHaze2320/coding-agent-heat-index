# 🔥 Coding Agent Heat Index

The daily leaderboard for AI coding agents. Cursor, Claude Code, Cline, Aider, Codex, Gemini CLI and more — ranked by real momentum, not marketing.

*Building… the first run will populate this README with the full leaderboard. Run `python tracker.py && python dashboard.py` or trigger the GitHub Action.*
